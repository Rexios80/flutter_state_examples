name: flutter_state_examples
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  fast_rx_shared_preferences: ^0.1.4
  fast_ui: ^0.1.2
  flutter:
    sdk: flutter

dev_dependencies:
  rexios_lints: ^5.0.0

flutter:
  uses-material-design: true
